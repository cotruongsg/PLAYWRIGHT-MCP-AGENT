# Playwright-MCP-Agent

This project is a Playwright setup for testing web applications.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Run tests:
   ```bash
   npx playwright test
   ```

## Folder Structure

- `tests/`: Contains all test files.
- `playwright.config.ts`: Configuration file for Playwright.
